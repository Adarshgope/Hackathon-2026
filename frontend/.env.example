# Point the frontend at the Flask API. Defaults to http://localhost:5000.
# On macOS port 5000 is taken by AirPlay Receiver — run the backend on another
# port (PORT=5050 python run.py) and set this to match.
VITE_API_URL=http://localhost:5000
